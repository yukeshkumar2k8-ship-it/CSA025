#include <stdio.h>
#include <string.h>
int main(){
    char a[100],b[100],temp[100];
    gets(a);
    gets(b);
    strcpy(temp,a);
    strcpy(a,b);
    strcpy(b,temp);
    printf("%s\n%s",a,b);
    return 0;
}