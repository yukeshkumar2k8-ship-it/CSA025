#include <stdio.h>

int main() {
    float radius, area;

    // Input
    printf("Enter radius: ");
    scanf("%f", &radius);

    // Calculate area
    area = 3.14 * radius * radius;

    // Output
    printf("Area of circle = %.2f\n", area);

    return 0;
}
